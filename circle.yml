machine:

  timezone:
    America/Toronto

  node:
    version: 6.11

  compile:
    override:
      - npm build

  test:
    override:
      - echo "done"

  deployment:
    staging:
      branch: develop
      commands:
        - ./deploy_staging.sh
